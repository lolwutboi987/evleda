# Native schematic style seed v1

`manifest.json` publishes eight portable template files (62,918 bytes) and the exact required two-field runtime materialization recipe. The blank template is **not native-byte-stable**: its authorized native export succeeded but rewrote those two empty values. A separate authorized repeat, with those values materialized before capture, preserved the entire configuration entry set, bytes, SHA-256, device IDs and file IDs. All 23 recorded SVG body measurements matched. See `provenance/native-verification.json` for evidence identities and limits.

Files live at `config/<relativePath>` and must match the manifest's `sizeBytes` and SHA-256 before use. Create all `configDirectories` under the owned configuration root, including empty `10.0/colors`. Create `cacheDirectories` separately under the owned cache root. Empty directories may not survive packaging; the consumer must recreate them. Do not add placeholders inside the configuration tree.

In a fresh owned copy, replace only the JSON string token for `/api/interpreter_path` in `10.0/kicad_common.json` with the approved KiCad CLI's sibling `pythonw.exe` absolute path, and `/system/working_dir` with the exact owned native export working directory. Both original values must be empty strings. JSON-escape the replacement strings, preserve every other byte, and perform this operation before capturing the owned instance's identities. Reject missing, duplicate or unexpected tokens; do not reserialize the whole document. Template hashes apply before materialization; separately capture and verify materialized instance hashes afterward. These are the only authorized template substitutions.

The successful native evidence used a private config/cache/home/temp environment and preseeded project-local preferences. Existing SCH/PRO/PRL files and the two recorded native binary identities remained unchanged. Native export still emitted Fontconfig `SHGetFolderPathA` warnings and added private project/user-library directories and cache entries. Configuration stability does not imply project/cache topology stability, warning-free rendering, arbitrary-host stability, or full adapter workflow verification. The historical blank repeat and all private side effects are retained.

All eight JSON files were reviewed for machine paths, user/global imports, histories, author fields and secret-like content. Only the two original host paths required blanking; the other seven files retain exact native-generated bytes. Histories and author fields are empty; no populated user environment overrides or secret-like values were found. Stock relative BOM plugin filenames, KIPRJMOD/REFERENCE macros, the official KiCad repository URL and Windows Explorer command remain native defaults. This content audit does not establish independence of the legacy native generation from every global setting: that older run inherited a broader environment.

Attribution: configuration output generated by KiCad 10.0.3 (KiCad developers) from the EvlEDA synthetic application seed (`meta.version` 3, default line thickness 6). No executable or upstream source is vendored. `candidate-manifest.json`, `provenance/audit.json`, and `provenance/creation-chain.json` are preserved historical records from before the authorized repeats; their pending language describes that earlier stage. Current publication evidence is `manifest.json` and `provenance/native-verification.json`. Evidence locations are never runtime fallbacks.
