# DOC10 schematic cardinal correction

These are exact source references copied from the independently reviewed DOC10
qualification artifacts. They change only schematic pin-coordinate rotation and
the corresponding visual geometry rotation on DOC9. Runtime relocation may also
change only the `home` line in `environment/pyvenv.cfg`.

The verifier authenticates every predecessor publication, these two source
leaves, patch and qualification receipt/report before invoking the existing full
runtime closure verifier. No extra files, bytecode, changed native dependencies,
policy changes or DOC8 no-connect filtering are admitted.

The receipt qualifies registered public read-tool bodies and exact aliases
against an independent native CLI oracle. It does not certify GUI/IPC operation,
an imported project, whole native graph equality, or PCB acceptance. The trusted
runtime source importer retains that distinction and requires a normal new
project lifecycle; existing projects and runtimes are never edited by this
publication.

`profile-admission-02/` retains the corrected qualification revision separately.
The original `-01` runtime geometry/closure observations remain valid, but its
profile retained DOC9's path-derived launcher hash and failed real admission.
Revision02 changes only that derived hash, records real native/design-reader
admission, and keeps all flags, argument count, bytecode policy and runtime bytes
unchanged. The high-level verifier now authenticates this revision as well; the
original publication and failed-import evidence remain preserved.
