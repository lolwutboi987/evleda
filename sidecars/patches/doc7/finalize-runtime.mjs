// Only the owned, unpublished DOC7 copy from the retained qualification failure
// is eligible; buildDoc7Runtime(true) reconstructs and checks its whole closure.
import { buildDoc7Runtime } from "./build-runtime.mjs";
await buildDoc7Runtime(true);
